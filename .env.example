# OpenCode Academic Enhanced - Configuration
# Copy this file to .env and customize before running: docker compose up -d

# Port mapping (host:container)
PORT=4096

# Local directory to persist chat history, API keys, and config
DATA_DIR=./data

# Local directory to mount as the AI's workspace
WORKSPACE=.
